# CyberDeck — Build Roadmap & Status

**Working file: `CyberDeck IDE (Phase 4).dc.html` — ALL phases are built into this one file, in place. Never fork a new file unless explicitly asked.**

When the user says "continue", resume at the first phase not marked ✅, then update this file.

CyberDeck is NOT Figma — it's a Touch Portal + Stream Deck + Node-RED hybrid. Components are functional: they own bindings, states, actions, variables, runtime behavior, plugins, flows.

<!-- RESUME POINT: Phase 33 (Platform Architecture, steps.md "Phase 5") ✅ built 2026-07-13. Remaining backlog: AUDIT.md §8 items — module extraction (H5), accessibility pass 2, live document mirroring (M11) — plus any steps.md follow-ups the user requests (e.g. more Platform Notes, extension marketplace surface). -->

## Status

- ✅ Phase 1 — Integration Matrix (separate early file)
- ✅ Phase 2 — Information Architecture (separate early file)
- ✅ Phase 3 — IDE shell (workspaces, palette, prefs, wizard, themes)
- ✅ Phase 4 — Interaction polish (tour, panel toggles, keyboard, toasts)
- ✅ Phase 5 — Layers Enhancement: drag reorder, nesting, groups (⌘G/⇧⌘G), lock/hide, color labels (context menu swatches), context menu, rename, filter chips, search
- ✅ Phase 6 — Canvas Interaction: free drag, 8-handle resize, rotation handle (15° snap, ⇧ free), smart guides + edge/center snapping (snap toggle in status bar, ⇧ disables), arrow-key nudge (⇧=10px), live X/Y/W/H/R/O inspector sync (editable), constraints section, cursor readout in status bar
- ✅ Phase 7 — Selection Engine: canvas multi-select (⇧/⌘-click), marquee (drag empty canvas), lasso (⌥-drag), multi-drag, selection history ([ / ] keys), Tab/⇧Tab cycles widgets, Esc deselects, multi-select inspector (align 6-way, distribute H/V, group)
- ✅ Phase 7.5 — Component Foundation: Create Component (right-click / ⌘⌥K) converts Group → Component. ONLY: component icon/badge/name, jump to master, edit master. Component + Instance, nothing else (no variants/overrides/symbols yet)
- ✅ Phase 8 — Advanced Inspector: Component section in Design inspector (shows for master/instance selection): purple Main/Instance tag, striped preview thumbnail, editable name (syncs layer + Project Explorer registry), description, category cycler, Exposed Properties + Exposed Variables toggles, Update thumbnail, Find all instances (with count), Go to Master + Detach Instance (instance only, fields read-only)
- ✅ Phase 8.25 — Component Browser & Insertion: Insert tab in Design left panel (instant + fuzzy + tag search e.g. "music", category sections w/ collapse, Recent + Favorites chips, star favorites, plugin-aware sections OBS/GitHub/Spotify/Discord with PLUGIN badge + green dot), ~60 components with realistic placeholder content, hover preview card (live thumbnail, description, complexity, states, presets), 6 insertion paths: drag-drop to canvas, double-click, canvas right-click Insert menu, toolbar Insert button, ⌘K palette "Insert …" rows, `I` shortcut. Quick-setup preset popover after placement (Button variant/size, Gauge circular/linear, Chart line/bar/pie). Inserted widgets get layer rows, selection, inspector sync. Library → Components tab is driven from the SAME registry (single source of truth): auto-generated category chips + sections, search, plugin badges, hover preview, double-click inserts into Design
- ✅ Phase 8.5 — Shared Styles: registry of Fill / Stroke / Typography / Effect / Radius styles. Design inspector "Shared Styles" section with per-group link chips → style picker popover (swatches, live ref counts, detach, new-from-selection); applies to multi-selection. Library → Styles tab (same registry): grouped cards with previews, mono token values, ref counts, recolor dots on fill styles that PROPAGATE to every linked widget. Palette: "Create Shared Style from Selection" + "Open Styles Library". Built before variants, as planned
- ✅ Phase 9 — Variants: per-component variant registry (seeded Default/Success/Warning/Danger/Disabled/Pressed with theme colors). Master inspector shows a Variants list — swatch + editable name (double-click) + token + delete, set-default, Add variant. Instance inspector shows a Variant swap segmented control + active tag; swapping is per-instance. Instances carry a colored variant dot in the Layers panel; new instances inherit the master's active variant. Context menu "Swap Variant" cycles. Deleting a variant remaps affected instances. Each variant now carries {name,color,icon,label,state}: master inspector shows a focused-variant preview thumbnail + per-variant property table (Label text field, Icon cycler, Fill swatch picker that propagates to linked instances, State cycler), chips show the variant icon + a pin to set default, focus a chip to edit it. "Apply to all instances" button bulk-sets every instance to the default variant (live count). Keyboard , / . swap the selected instance's variant prev/next
- ✅ Phase 9.5 — Overrides: instance inspector "Overrides" section — per-field override of Text, Icon, Color, State, Variable, Action, Visibility, Padding without detaching. Each row shows a purple dot + revert-to-master (↺) when overridden, with a live count and "Reset all". Overrides are stored per-instance (keyed by lkid), reflect on the canvas/layers where sensible (color→variant dot, visibility→dim + off, text→layer name), and never touch the master or sibling instances. Master keeps the Exposed Properties/Variables list
- ✅ Phase 10 — Nested Components: components can live inside other components. Instantiating an outer component deep-instantiates its subtree — every nested container gets fresh unique ids, nested component masters become instances of their original master (registry counts bump, variant dots inherited), and nested instances keep their master link with fresh ids and no carried-over overrides. Component inspector shows a "Nested in A › B" breadcrumb of component ancestors. Groups containing components stay convertible with no refactor. Board seeds a live demo: "Perf Group" is a component containing the "Stat Cluster" component, with one collapsed instance showing the deep-instantiated nested instance
- ✅ Phase 10.5 — Symbols: lightweight reusable asset registry (Icon / SVG / Lottie / Animation) surfaced as a new Library → Symbols tab — type-grouped grid with animated previews (spin for Lottie, pulse ring for Animation, striped fill for SVG), type badges, tag chips, favorites, live use-counts, and type + text search. Double-click drops a symbol tile onto the Design canvas (adds a SYM-badged layer row, bumps the use-count live). Command palette: "New Symbol from Selection" + "Open Symbols Library". Symbols are distinct from Components — visual assets, not functional widgets
- ✅ Phase 11 — Assets ↔ Components: asset browser ↔ component browser integration (PNG → Image Widget → Create Component, SVG → Button, Lottie → Animated Widget)
- ✅ Phase 12 — Flows target Components: flow nodes bind to the component system instead of raw widgets. Each node carries a kind badge (Trigger / Condition / Action) and a purple target chip on the canvas. Selecting a node rebuilds the Flows inspector with a **Target** section: Component picker → Instance picker → Target-kind segmented control (kinds adapt to node kind — triggers watch Variable/State/Property, conditions read Variable/Property/State, actions set Variant/State, call Action, or set Property/Variable) → Value picker. A resolved binding line (`Component › Instance` + verb → value) + node badge + Last-Run trace all sync live. Component catalog (Scene Button, GPU Gauge, RGB Widget, Stat Cluster, Notify Toast) each expose instances/variants/states/actions/properties/variables; changing component resets instance+value. Pickers reuse the shared popover; toast confirmations throughout. **Drag-connect:** each node has an output port (right) and input port (left); drag from an out-port to another node to wire an edge (purple drop-target highlight, dashed ghost while dragging). Edges are rendered from a live model (bezier paths, animated dash, true/false branch labels off conditions); hovering an edge reveals a × to disconnect. Condition sources auto-assign true then false branches. Connecting/disconnecting updates the node's Connections list + toast.

## v2 Parity — remaining work to match the Designer v2 mockup

These bring the IDE up to full parity with `CyberDeck Designer v2.dc.html`. Resume at the first ○.

### Flows mode
- ✅ Phase 13 — Flows foundation: multiple-flow model (`flowsModel()` with per-flow nodes/edges/armed) + flow tabs (click to switch, double-click name to rename inline) + New Flow (+ tab, seeds a trigger) + per-flow Armed dot toggle. Nodes/edges/palette now render from the model (`renderFlowTabs`/`renderFlowGraph`/`renderFlowLib`); inspector, edges and badges rewired to the active flow. Node-library palette rebuilt (6 categories, searchable). Empty-graph flow inspector shows flow name + counts
- ✅ Phase 14 — Flows editing: colored node kinds (kind-tinted top border + colored icon chip + kind badge for Trigger/Condition/Action, plus Integration/Data/Structure palette colors) + palette drag-to-add (drag a node from the left library onto the graph to drop it at the cursor; graph shows a drop highlight; double-click a palette node adds it too) + node dragging (pointer-drag any node, edges redraw live via the model, 3px threshold preserves click-to-select)
- ✅ Phase 15 — Flows edges & inspector: branch-colored edge arrowheads (green true / red false / accent default / white selected, SVG markers) + edge select (click the edge, white-highlighted, dedicated Edge inspector with endpoints + Delete-edge button) + edge delete (× hotspot, inspector button, or ⌫/Delete key when a flows edge is selected) + condition-branch toggle in the edge inspector (true/false/always) + per-node-kind inspector Parameters section (trigger edge/debounce/once, condition op/match/negate, action & integration retry/delay/timeout/await, notify priority, structure group-mode — segmented + numeric + text controls, persisted on the node) + empty-canvas click clears selection and shows the flow-level inspector
- ✅ Phase 16 — Flows Test Run: "Test Run" button in the flow toolbar simulates the active flow with a graph-walk animation — computes execution order (roots = triggers / nodes with no inbound edge, DFS along edges), pulses each node green in sequence (`.fnode.run` → `.fnode.done` check), animates each traversed edge (`.run2`), dims un-executed nodes, and streams a live step log into the inspector (kind-colored rows + timing). Stop button / auto-stop on flow switch or workspace change; "Run again" to replay

### Design mode
- ✅ Phase 17 — Canvas navigation: board wrapped in a transformed `.world` layer; zoom via ⌘/ctrl-wheel (to cursor), zoom-in/out buttons, ⌘+ / ⌘− / ⌘0, and click-the-percentage to fit; plain wheel pans; Hand tool (toolbar, `H`, or hold `Space`) drag-pans with grab cursor; dot-grid tracks pan/zoom; widget drag/resize/rotate, cursor readout, and component drop all divide screen deltas by the zoom factor so editing stays accurate at any zoom. `V` returns to the move tool
- ✅ Phase 18 — Minimap: live minimap docked bottom-right of the canvas showing the board outline, every widget block (selected one accent-filled), and a green viewport rectangle; updates on pan/zoom and on any widget move (via `updateMinimap` from `syncSelBox`/`applyWorld`); click or drag anywhere in the minimap to recenter the viewport there
- ✅ Phase 19 — Selection minibar + Live Mirror: a floating minibar (Duplicate / Lock / Group / Delete / More) appears above any canvas selection, tracking it live at constant screen size through pan & zoom — Duplicate clones widgets with a +16 offset, Lock toggles a per-widget lock (blocks drag/resize, shows a lock badge, icon flips to unlock), Group runs the existing grouping, Delete removes widgets + their layer rows, More opens the full context menu. Plus a draggable "Live Mirror" floating panel that renders a live scaled wireframe of the board (selected widget highlighted) and can be dragged anywhere within the canvas
- ✅ Phase 20 — Bindings: per-field binding popover with Static / Variable / Expression modes. Hovering an inspector layout field (X/Y/W/H/R/O) reveals a link icon that opens the popover; a dynamic Bindings section lists Value/Visible (and any bound layout props) with mode badge + live value and opens the same popover. Variable mode shows a searchable live variable catalog (Telemetry/OBS/Media/Runtime, grouped, with live values); Expression mode has a textarea + one-tap variable-insert chips and a live-evaluated preview (real JS eval of the expression against variable values, error state on failure). Bound layout fields lock to a chip, widgets show an accent bind-dot, and all bindings persist to localStorage (restored + re-applied on load). Esc / outside-click closes the popover
- ✅ Phase 21 — States & Events: interactive States section per widget — state chips (Default/Hover/Pressed/Focus/Disabled + custom via prompt), an override editor (Opacity/Scale sliders + Glow toggle) whose changes become per-state overrides (accent dot + reset), and switching a chip previews that state live on the canvas widget (opacity/scale/glow, Disabled grayscale). Events section lists On tap / hold / value-change / double-tap rows; clicking one slides up a flow drawer showing a Trigger→Action mini-graph and a flow picker to assign/disconnect a flow (or jump to the Flows workspace). States, overrides and event wiring all persist to localStorage; Esc closes the drawer
- ✅ Phase 22 — Density & layers: Beginner/Power density toggle in the canvas toolbar (Beginner hides pro-only sections — Alignment, Bindings, States, Events — and shows a hint in the Layers panel; persists to localStorage). Layers breadcrumb segments are now clickable to jump-select any ancestor layer/widget. Collapse-all / expand-all buttons in the Layers filter row fold or unfold every container at once

## v3 — IDE Cohesion (from AUDIT.md, 2026-07-07)

Source: `AUDIT.md` — full UX & architecture audit. Goal: one cohesive desktop IDE, no new features. Resume at the first ○.

- ✅ Phase 23 — Consistency & Dead-Control Sweep: wired-or-honest-stubbed every dead control (C5); deduped the six conflicting CSS blocks — removed the Phase-13 `.minimap/.mm-stage/.mm-art/.mm-node/.mm-view/.minibar/.floatp/.fp-h2/.fp-mini/.canvas.pan` duplicate block (kept the wired Phase-18/19 defs: minimap bottom-right w/ collapsible header + green viewport, Live Mirror top-right) and the redundant earlier `.fnode.run` (H8); added a token scale to `.app` — `--r-s/m/l/pill`, `--h-s/m/l`, and a full `--z-panel…--z-pick` z-index scale, applied the z-index tokens to every overlay (fixes the toast/bpop 120 collision: bpop=popover 120, toast=130, tour=135) (M9/M5); harmonized the two underline-tab patterns (`.dtabs s` now matches `.itab`: 11×13 padding, 12.5px) (H7); allowed text selection in the Runtime log + Vars values (L9). **Deferred:** `<s>`→`<span>` semantic rename (L3) — `s` is a load-bearing structural/click hook across ~12 CSS selectors (`.segc s`, `.kv s`, `.dtabs s`, `.pt-h s[data-sortcol]`, `.bmodes s`, `.fpseg s`…) and JS delegation; renaming risks broad breakage for a Low-priority a11y-only gain. Full tab/segment class *consolidation* into `.tabs`+`.segc` (H7 full) also deferred to avoid churning JS selectors; values were harmonized instead.
- ✅ Phase 24 — Central AppState & Selection Manager: added `this.app` store `{workspace,selection,zoom,density}` + `subscribe(fn)`/`notify()`. `goWs` now routes through `setWorkspace()`→notify+saveSession; `selChanged`/layer-select route through `refreshApp()` which derives selection from the DOM and notifies. Two subscribers registered at mount: **syncTopCrumb** renders the top breadcrumb from AppState — clickable `Battlestation › <Workspace> › <selection>` (Battlestation→Projects, selection label shown when present) — and **syncStatusBar** which (a) toggles `ws-<id>` classes on the status bar + root and (b) fills a ref'd context segment per workspace (flows: node count + armed; runtime: events; vars: N variables; etc). Design-only status segments (cursor readout, snap toggle) and the selection segment are gated by CSS on the sbar's `ws-*` class (M2). Session persistence in one `cdk-session` key (workspace, density, libTab, zoom) written on workspace/density/libTab change and restored on mount (falls back to `startWorkspace` prop). **Impl notes for future phases:** (1) the status bar and overlays now render correctly *inside* `.app` (fixed 2026-07-07: an extra `</div>` in the Integration Center section — three closes after `.center` where two were needed — was prematurely closing `.body`, hoisting `.rpanel`/`.sbar`/overlays out to `.sc-host`; removing it restored the tree, zero stray `.rpanel`s). `syncStatusBar` still resolves the sbar via `sbCtxRef.closest('.sbar')` which works regardless. (2) The DC runtime reverts imperative `textContent`/`style` on template-managed nodes (those with `data-dc-tpl`); reliable escape hatches are ref'd-element `.innerHTML` (like the crumb) and `classList` toggles on elements — used both here.

<!-- RESUME POINT: all phases complete — see the top-of-file note for next steps (AUDIT.md §8). -->

### Deferred within completed phases
- Phase 23: `<s>`→`<span>` semantic rename (L3) and full `.tabs`/`.segc` class consolidation (H7) — both defer to avoid churning ~12 load-bearing `s` CSS/JS selectors; values were harmonized instead.
- ✅ Phase 25 — Command Registry & Undo/Redo: `CMDS()` is the single command registry ({id,g,label,icon,keys,act,global}); it drives (a) the keydown handler via `cmdDispatch(e)`+`matchCombo` (all global ⌘-combos resolve through the registry — palette, prefs, undo, redo, wizard, togL/R, timeline, drawer, themes, integ), (b) the Preferences → Keyboard pane rendered grouped via `renderKbReg()`, and (c) Undo/Redo palette rows. **Undo/Redo engine:** snapshot-based history (`_hist`/`_hi`) — `snap()` captures canvas widgets (`.board .wt` outerHTML) + Layers pane HTML, `runCmd(label,icon,mutate,coalesce)` diffs before/after and pushes `{label,icon,before,after,time}`; `undo`/`redo` (⌘Z/⇧⌘Z) restore snapshots, `histJump(i)` steps to any point, `undoAll()` restores baseline. Wired mutations: widget dup/del, layer dup/del, group/ungroup, arrow-key nudge (coalesced <1s), drag-move/resize/rotate (`_opBefore`/`endOp`, one command per gesture). **Timeline panel** fed live from history — newest-first edit steps (icon+label+time), current dot, undone steps dimmed, click a row to jump; Undo/Redo + Restore-baseline buttons. **Toast undo affordance:** `toast(msg,undoable)` shows an inline Undo button on every history-producing action. Dead Phase-25 stubs (Timeline Compare/Restore, kbrow rebind) now wired/honest. **Impl note:** snapshot restore uses `_gv` (the smart-guide element, appended to `.board` after widgets) as the re-insert anchor so restored `.wt` land between the board tag and the guide/selbox overlays. (C1, M14)
- ✅ Phase 26 — Contextual Inspector: `syncInspContext(rp)` (called first thing in `syncCompSec`, so it runs on every selection path) derives the selection kind and rebuilds the Design inspector to match. **Type detection** `typeOf(w)` maps inserted widgets by `data-cbid`→`CBKIND` and seed widgets by `data-lname`→`SEEDKIND` (fallback DOM sniff) into 10 kinds (gauge/stat/button/text/chart/image/media/input/container/generic). A new **type section** `[data-typesec]` renders only the props relevant to the kind — Gauge: value binding + arc style + min/max/warn + ticks; Button: label + icon cycler + action + style + haptic; Text: content + font + size + weight + align; Chart: series + type + window + fill; Stat/Image/Media/Input/Container each their own — built from reusable `_tsSeg/_tsVar/_tsNum/_tsTog/_tsSel/_tsTxt/_tsArea` row helpers, state stored per-widget in `_tprops`. The title becomes "Kind · Name". **Empty selection → Page Properties** `[data-pagesec]` (artboard preset, grid Off/Dots/Lines wired to the board bg-image, cols×rows, background swatches wired to board bg, snap toggle) via a new `rpanel.nosel` state that hides all widget sections. **Multi** keeps the existing align/`msec` path untouched. **Beginner mode (M13):** instead of pro-only sections silently vanishing, a `[data-advstub]` "Advanced" section (`.beg-only`) shows a lock card with live binding count + a "Switch to Power mode" button. Interactions: segmented/toggles/cyclers handled by `typeAct`/`pageAct` (routed early in `onDown`, before the generic `.segc`/`.sw` handlers); text fields via `onTypeInput`/`onPageInput`; the value chip opens the real bind popover (`openBindPop('value',…)`). (H1, M13)
- ✅ Phase 27 — Panel System v1: **Resizable L/R panels** — `.lpanel`/`.rpanel` widths driven by `--lpw`/`--rpw` CSS vars on `.app`; two absolutely-positioned `.reszh` drag handles (children of `.body`, `position:relative`) positioned live by `layoutHandles()` from the active visible panel's rect (hidden when a panel is absent/hidden, e.g. Projects Home) — dragging (`startPanelResize`) clamps 180–480px, live-updates minimap+mirror, and persists per-workspace. **Layout store** `cdk-layout` (`loadLayout`/`saveLayout`) keyed `{ws:{<id>:{lpw,rpw,hideL,hideR}}, mirror:{hidden,dev,corner,x,y}, minimap:{hidden,clps}}`; `applyWsLayout(id)` (called from `goWs` + mount) restores per-workspace widths + panel-hidden state (so each workspace remembers its own layout), `togPanel` persists hidden state. **Live Mirror + minimap as tool windows:** close (×) buttons in both headers, `.mhide` hide class, restore via status-bar toggle chips (`data-sbseg="design"`, live `.on` state via `syncPanelToggles`) + `⌘K` palette rows + `⇧⌘M`/`⇧⌘L` global shortcuts (in `CMDS()` → Keyboard prefs). Minimap collapse (`clps`) now persists. **Snap-to-corner:** dragging the mirror within 68px of a corner shows an accent snap outline (`.floatp.snap`) and snaps to a 14px corner inset on release (`nearCorner`/`cornerPos`); free position + corner persisted and restored (`applyMirrorPos`). **Live Mirror device switcher:** header chip cycles 4 devices (`MIRDEVS`: iPad/iPhone/Deck XL/Mini) — updates header icon (span+innerHTML re-render so it survives lucide), device chip, foot label + latency; index persisted. Mirror already renders real board widget blocks. (C4, H9)

- ✅ Phase 28 — Flows Graph Parity: **Shared PanZoom surface** — `makePZ(surface,world,opts)` factory (pan/zoom/apply/zoomAt/panBy/fit/wheel) now backs BOTH the Design canvas (`this._cpz`, with `get/set pan`+`get/set zoom` delegating to it so all existing Phase 17-19 call sites keep working) and the Flows graph (`this._fpz`). **Flows world:** `.fgraph` now wraps nodes + edge SVG in a transformed `.fworld` layer (`pointer-events:none`, nodes/edge-hitpaths re-enabled) — ⌘/ctrl-wheel zoom-to-cursor, plain-wheel pan, Hand/Space drag-pan (`startFPan`), a bottom-left zoom toolbar (`[data-fzoom]` −/label/+/fit), ⌘+/−/0 gated to the active workspace, and first-open zoom-to-fit (`fZoomFit` frames all nodes). Drop-to-add and edge-drag ghosts convert screen→world via `fgWorldPt`. **Marquee + multi:** dragging empty graph rubber-bands (`startFMarquee`, client-rect intersection, shift=additive); marquee/⇧-click multi-select shows a Selection inspector (`flowMultiInsp`); dragging any selected node moves the whole selection together (deltas ÷ fzoom); ⌘D duplicates selected nodes (+internal edges, +28px offset) and ⌫/Delete removes them. **Per-port geometry (H3):** `portXY(fnid,which,branch)` now reads the node element's live `offsetWidth/offsetHeight` so ports anchor to the right edge + vertical center and track node height; conditions render **two out-ports** (green `T` top / red `F` bottom, at 36%/64%) — `startEdge`/`addEdge` carry the branch from the originating port so true/false edges leave distinct points. (H2, H3)

- ✅ Phase 29 — Data-Driven Workspaces: **Variables** — scopes rail (`renderVarScopes`), table (`renderVarsTable`) and inspector (`buildVarInsp`) all render from `varRows()`/`VARCAT`; search (`onVarSearch`), scope + plugin filters (`data-varscope`), row-click select→inspector, inline value edit (`data-vv`, plugin-owned read-only), live ref counts. **Projects** — `.ptable` body now `[data-pttable]` + browse inspector now `[data-projinsp]`, both fed from the `PROJECTS()` model via `renderProjTable`/`buildProjInsp`, home cards via `renderPjGrid`; column sort (`projSort`, keys name/mod/flows/vars/online with header arrows), row select drives inspector, `data-projmenu` row actions, `data-projopen` → `openProject` (jumps to Design). **Board model** — `boardModel()` now measures every `.board .wt` (grid-seeded AND absolute) via bounding rects ÷ zoom into unscaled board-local coords and caches to `_boardSnap` (so hidden-workspace reads still work); device mocks (`renderDeviceMocks`), minimap (`updateMinimap`) and Live Mirror (`updateMirror`) all render from that single model. Removed the stale DOM-sort `sortTable` + duplicate `.pt-r`/`.vrow` onDown handlers that shadowed the model paths. (H10, M10, M11)
- ✅ Phase 30 — ProjectModel Migration: extracted a real document model into a helper module `cyberdeck-model.js` (`window.CDKModel = {uid, ProjectModel}`, loaded via a plain `<script src>` in the helmet). **Stable ids:** `migrateIds()` (run first thing in `initBoard`, before the registry restores) stamps a `data-id` on every seed `.wt`/`.lyr` and `key(el)` is the single identity accessor (prefers `data-id`, falls back to the legacy `lname`/`wname` slug so nothing breaks mid-migration); `stampId(el,id)` stamps a widget AND its paired layer together. Every creation path now stamps `data-id`: `insertCb`, symbol drop, asset drop and widget duplicate set `data-id=lname` (widget id === lname invariant kept, so all `[data-lname]` DOM lookups and `applyBinding` selectors stay valid), group containers get `data-id=lkid`, and layer/group duplicates get FRESH `newId()`s instead of the old `delete dataset.lname` identity-wipe (fixes "duplicate corrupts state"). **Registries re-keyed on id:** all ~22 `x.dataset.lname||x.dataset.wname` identity derivations swapped to `this.key(x)` (binds/states/events stores, selection ids, pushSel/swapSel history, tprops, advstub, buildBindSec/syncBindFields/openBindPop, applyStateVisual, stAct). `_binds`/`_states`/`_events` now live ON the `ProjectModel` instance (`this.PM`); `pmInit()` aliases them and `persist*/restore*` delegate to `PM.persist/PM.restore` (legacy `cdk-binds/states/events` keys preserved for back-compat). **Locks re-keyed on id:** `persistLocks`/`restoreLocks`/`key` route through stable ids (`cdk-locked` now stores ids; restore resolves `[data-id]` then legacy `[data-lname]`), fixing the display-name collision (C3). **Serialization path (the missing piece in C3):** `serializeProject()` → `PM.serialize(projectWidgets(), meta)` emits a full `{format:'cyberdeck.project', version, savedAt, meta, widgets[], registries}` doc (widgets measured from the board with id/name/type/variable/locked/x/y/w/h); `exportModel()` writes it to `cdk-project` + downloads `battlestation.cdk.json` + toasts stats. New command `exportModel` (⇧⌘E) in `CMDS()`, the ⌘K palette (Project group) and Keyboard prefs. **Verified:** module loads, 8/8 widgets + 15/15 layers carry unique ids, export produces a valid versioned doc. **Deferred (consistent with prior module-extraction deferrals):** full flows-model / component-registry extraction into their own modules — those methods are deeply `this`-bound (toasts, `this.$`, DOM mutation) so only the pure binding-store + id-keyed registries were extracted this phase; and a `render(dirtyIds)` DOM reconciler (DOM still renders imperatively, model is now authoritative for identity + persistence + serialization). (C3, H5)
- ✅ Phase 31 — Accessibility & Input Polish: **Focus-visible (H6):** global `:focus-visible` outline ring on every interactive class (`[tabindex]`, `[role=button]`, `[role=tab]`, links, inputs, contenteditable), `:focus:not(:focus-visible)` suppressed, plus a `prefers-reduced-motion` guard that near-zeroes animation/transition durations. **Programmatic a11y stamping:** `a11yInit()`/`a11yStamp(root)` walk `A11Y_SEL` (`[data-act]/[data-ws]/[data-tool]/[data-zoom]/[data-density]/.rb/.iconbtn/.tool/.btn/.cm-row/.pal-row/.tpl/.dcard/.chip/.wbtns i`) and stamp `tabindex=0` + `role` (button, or tab for `.dtabs s/.tabs s`) + `aria-label` (from title → text → data-act) once per element (`data-a11y` guard); `.tbar`→role toolbar, `.rail`→role navigation, root→aria-label. Verified 9/9 rail buttons + 23/23 icon buttons + 23 context-menu rows stamped. **Keyboard activation (H6):** `activateFocused()` fires pointerdown+click on the focused control for Enter/Space (gated to stamped/button/tab roles, never inputs) so the whole chrome is operable without a mouse — verified Enter on a focused rail button switches workspace. **Focus-trapped modals (H6/M12):** `openOvl` records `_lastFocus`, sets `role=dialog`+`aria-modal`, and focuses the primary action (`[data-wnav=next]`/`.btn.pri`/first field); a Tab handler in the global keydown cycles focus within the open `.ovl` (shift reverses, wraps); `closeAll(keepFocus)` restores focus to `_lastFocus` on close (openOvl passes keepFocus to avoid clobbering on re-open) — verified palette traps Tab and Esc restores focus. **Toast policy (M4):** removed ambient-success toasts for theme apply, density switch and snap toggle (state is already reflected in the status bar / toolbar / Layers hint; snap button now carries `aria-pressed`); transient error/hint toasts and undoable-action toasts kept. **Esc cancels rename (L):** layer rename, variant rename and flow rename now capture the original text and restore-on-Escape (flow rename also suppresses the false "renamed" toast via a `cancelled` flag) + each gets `role=textbox`+aria-label; var-value edit already had Esc. **Tour target guards (L):** `tourStep` now skips a step whose target selector is missing (advances to the next step / ends gracefully) instead of aborting the whole tour on the first absent element. **Capability-driven context menu (M3):** confirmed the layers/canvas context menu already hides irrelevant rows per selection kind (mkcomp/mkinst/master/detach/swapvar/ungroup gated by `isG/isC/isI`) — left as-is. **Deferred:** full `role=tree`/`role=tablist` semantic tree + roving tabindex (H6 Pass 2), and wizard field-validation/dirty-guards for the canned demo wizard (no required user input to validate) — noted for a later a11y pass. (H6, M12, M4, M3, L6–L8)

- ✅ Phase 32 — Docking Engine (C4 full): tool windows (Live Mirror + minimap) dock to left/right/bottom rails of the design `.center` host. `dockInit()` reparents both windows out of the clipped `.canvas` into the `.center.dockhost`, injects a `.dockzones` overlay (left/right/bottom/float drop targets) + one auto-hide `.docktab` per window. **Drag-to-dock:** dragging a window header (`startDockDrag`) floats it and reveals the zones (`showZones`/`hotZone` highlight), dropping on a zone calls `setDock(id,side)`; dropping on float/empty re-floats with persisted fractional pos (`fx/fy`). **Canvas inset:** `applyDock()` sums pinned rail sizes into `--dock-l/-r/-b` and applies them as inline `!important` margins on `.canvas` via `setCanvasInset` (the DC runtime reverts template-managed styles, so inline-important is the escape hatch); `placeDocked()` positions each docked rail flush to its edge, splitting a side between co-docked windows. **Pin / auto-hide / peek:** header pin toggle (`toggleDockPin`) — pinned rails hold canvas inset; unpinned rails collapse to `autohidden` + show the edge `.docktab`, which peeks the window on hover (`.peek`, no inset) and re-pins on click. **Rail resize:** per-rail `.dock-rz` handle (`startDockResize`, 140–460px clamp, col/row cursor by side). **Float-back** via header float toggle (`toggleDockFloat`). All per-window state (`mode/side/pinned/size/fx/fy/clps/hidden`) persists in the `cdk-layout` `mirror`/`minimap` keys and restores on mount (`initLayout`→`applyDock`). Verified end-to-end: left/right/bottom dock + inset, pin↔auto-hide↔peek↔re-pin, resize, float-back, and the persistence round-trip; fixed a bug where a previously-float-collapsed minimap kept its `.clps` (blank body) when docked. (C4)
- ✅ Design workspace polish (2026-07-11) — **Floating inspector:** the Design-workspace `.rpanel` is no longer a rigid docked column; scoped by a `.dsgn` class on the design `<section>`, it floats over the canvas (absolute, top 54 / right 10 / bottom 102, rounded + blurred glass, z 34) so the canvas, ctxbar and asset strip run edge-to-edge beneath/around it. Sticky "Inspector" header (`.insp-h`) with a collapse button (`.insp-x`, `data-act="togR"`); when collapsed (`hideR`) a floating "Inspector" reopen chip (`.insp-reopen`, `.hideR .dsgn.on` gated) appears in its place. **Layout math made inspector-aware via `rpInset()`** (panel width + 20, 0 when hidden): `zoomFit` centers the board in the *visible* canvas band, `applyFloatPos` keeps default mirror/minimap float positions clear of the panel, `placeDocked` shifts right-docked rails + their auto-hide tabs left of the panel (tab drops 44px lower when the panel is collapsed so it clears the reopen chip), `.dz-right` dock zone shifts via `calc(var(--rpw)+26px)`, and `togPanel('R')` pans the canvas by half the inset delta so content recenters smoothly on collapse/expand. The right resize handle now tracks the floating panel's actual top/height instead of spanning full-height.
- ✅ Per-workspace panel presets: a status-bar **Layout** item (`data-act="layoutpreset"`, `layout-template` icon + live preset name, also `⌥⌘P` + a `⌘K` palette row) opens a popover (`.lpop`) of named presets rendered by `renderPresetMenu` with a mini L/C/R diagram per row + active check. Built-ins (`LAYOUT_PRESETS`): Balanced (default widths, both shown, tools floated), Focus (both side panels hidden), Inspector (narrow left / wide right), Explorer (wide left / right hidden), and a design-only **Docked Tools** (docks minimap bottom + Live Mirror right, pinned). `applyPreset` sets the active workspace's `--lpw/--rpw` + `hideL/hideR` (and, on design, the dock config via `applyDock`), persists to `wsLayout(id)` and updates the label. **Custom presets** — "Save current layout…" (`savePresetFromCurrent`, prompt-named) captures current widths/visibility (+ per-window dock state on design) into `wsLayout(id).presets[]`; saved rows carry a hover-delete (`deletePreset`). Presets are **per workspace** (each `cdk-layout.ws[id]` owns its `preset` name + `presets[]`), restored by `applyWsLayout`→`syncPresetLabel`; any manual panel resize/toggle or dock change routes through `markLayoutCustom()` and flips the label to "Custom". Verified: all built-ins apply, per-workspace independence (Flows vs Design), Custom-state detection, custom save/delete, and persistence round-trip.

- ✅ Device Player Preview (2026-07-13) — full-screen `data-ovl="devprev"` overlay: the *player* view of the created deck as it renders on an end device (per user: desktop = engine/IDE, phones/tablets = display only). Device chips (Living Room iPad 1180×820 / Pixel 8 915×412 / Deck Mini 640×400, `DPVDEVS`), portrait/landscape toggle + Rotate button (animated frame transition), board content rendered from `boardModel()` (extended with `name`+`kind` per node) as icon+label tiles (`DPVICON`, containers dashed), letterboxed fit inside the device screen, camera dot, footer readout (device · res · orientation · scale). Entry points: top-toolbar icon, Devices workspace "Player Preview" button (replaced the dead Rotate toast), ⌘K palette row, ⇧⌘P (in `CMDS()` → Keyboard prefs). Device+orientation persist in `cdk-dpv`; re-renders on window resize; Esc closes via existing overlay system. **Touch simulation (2026-07-13):** stage-level pointer handlers (`dpvDown/dpvUp/dpvFire`, guarded one-time bind on the stage ref) — press scale (`.press`), tap ripple at pointer coords (`.dpv-rip`), simulated haptic frame nudge (`.dpv-frame.hap`), tap vs 480ms hold detection, kind-specific verbs (`DPVVERB`), tap toggles an active state on button/media/input tiles (persisted per-index in `_dpvOn`, survives re-render/orientation swap), and a live event readout in the player footer (`[data-dpvevt]`). **Assigned layouts fix (2026-07-13):** player no longer depends on the live board DOM (was blank when Design ws not rendered) — `dpvModel(d)` returns the layout assigned to each device from `DPVLAYOUTS` (ipad "Main Deck" 1180×820 / pixel "Quick Actions" 412×915 / deck "Stream Panel" 640×400, seeded widget nodes with kind+name); iPad prefers the live `boardModel()` snapshot when it has nodes (footer shows "Main Deck · live board"); footer includes the layout name. Still to verify: all 3 devices × both orientations render, touch sim still works.

- ✅ Phase 33 — Platform Architecture (uploads/steps.md "Phase 5", 2026-07-13): the prototype now communicates a production platform architecture without changing the visual language — everything mocked, all dev-facing. **Architecture Mode** (status-bar "platform" chip, ⌥⌘A, ⌘K row, persisted `cdk-arch`): toggles `.arch` on the app root and reveals ~20 violet **Platform Note** markers (`.archdot`, JS-positioned by `archSync()` from `ARCH()` — each entry has `sels[]` candidate anchors incl. per-workspace ones; repositioned on goWs/resize). Clicking a dot opens `.archpop` with the exact spec format WHAT / HOW / WHY / FUTURE IMPLEMENTATION + meta chips + "Open Platform Inspector" link. Notes cover: Boot Manager, Configuration Loader, Workspace Manager, Command Registry, Service Container, Widget Registry, Dock/Layout Manager, Layout Presets, Theme Engine, Extension Host, Notification Service, Telemetry, Event Bus, Repository Layer, State Stores, Mock API Gateway, Background Services, Cache Manager, AI Platform, Plugin Host (Devices), Project Service. **Boot lifecycle** (`data-ovl="boot"`, `BOOTSEQ()` 10 stages with ms + notes): animated sequential replay (`playBoot`), rows click through to Platform Notes (`data-bootnote`), "interactive @142 ms" footer; auto-plays once per session (sessionStorage `cdk-booted`, only after tour seen), replayable via ⌘K "Replay Boot Sequence". **Platform Inspector** (`data-ovl="platform"`, ⇧⌘D, status-bar event chip): 7 tabs rendered by `pfTab()` — Services (13, DI container note), Repositories (mock request log w/ latency + cache HIT/MISS + capability chips), State Stores (13-row persistence map: kind/location/restore timing), Event Bus (live mock stream via `emitEvt`/`archTick` 4.2 s interval feeding the arch-only status-bar ticker `sbEvtRef`, + catalog w/ subscriber counts), Feature Flags (7 toggles persisted `cdk-flags`, emit SettingsChanged), Permissions (widget/extension declared-capability matrix), Loading & Perf (startup/lazy/background cards + performance + error-isolation notes). New CMDS group "Platform" (archmode/platform/bootreplay) so shortcuts appear in Keyboard prefs. goWs now emits WorkspaceChanged. All UI unchanged when Architecture Mode is off (chips gated by `.arch-only`).

## Integration rules (from user)

NO separate "Components" workspace. Components integrate into existing UI:
- Layers panel: right-click group → Create Component; instances get distinct icons + badges
- Project Explorer: top-level **Components** section (master library)
- Inspector: Component section — Go to Master, Detach Instance, Swap Variant, Expose Properties
- Asset Browser: Components category, drag to canvas
- Command Palette: Create Component, Create Variant, Detach Instance, Swap Component, Find All Instances
- Context menus everywhere (canvas, Layers, Project Explorer)

Groups must stay convertible to Components with no refactoring.

## Visual vocabulary
Dark cyber theme, CSS vars (--accent/--ink/--line/--panel/--asoft…), Rajdhani + JetBrains Mono + Exo 2, existing .srow/.lyr/.insp-sec/.cmenu/.tag patterns, lucide icons, toast confirmations.
