# golangci-lint v2 configuration for the CyberDeck engine module.
# The "standard" default set (errcheck, govet, ineffassign, staticcheck, unused)
# is the Phase-1 baseline; PROJ-102 tunes it as the codebase grows.
version: "2"

run:
  timeout: 5m

linters:
  default: standard
