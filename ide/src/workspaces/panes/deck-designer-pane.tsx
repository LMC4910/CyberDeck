// Placeholder pane for the Deck Designer workspace (CD-203). Real content arrives in
// M3/M4; this proves lazy code-splitting + the pane host. Its own chunk via import().
export default function DeckDesignerPane() {
  return (
    <section data-pane="deck-designer" aria-label="Deck Designer workspace">
      <h2>Deck Designer</h2>
      <p>The Deck Designer workspace arrives in a later milestone.</p>
    </section>
  )
}
