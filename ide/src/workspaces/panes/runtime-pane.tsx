// Runtime pane (CD-203 slot; content lands in CD-407/408). Replaces the Home
// placeholder — Runtime is the blueprint's seventh workspace (IDE-E15). Its own
// chunk via import().
export default function RuntimePane() {
  return (
    <section data-pane="runtime" aria-label="Runtime workspace">
      <h2>Runtime</h2>
      <p>The Runtime workspace arrives in a later milestone.</p>
    </section>
  )
}
