// Placeholder pane for the Variables workspace (CD-203). Real content arrives in
// M3/M4; this proves lazy code-splitting + the pane host. Its own chunk via import().
export default function VariablesPane() {
  return (
    <section data-pane="variables" aria-label="Variables workspace">
      <h2>Variables</h2>
      <p>The Variables workspace arrives in a later milestone.</p>
    </section>
  )
}
