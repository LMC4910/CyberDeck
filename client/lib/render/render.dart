/// Widget renderer registry + layout interpreter (PROJ-181). The designer canvas
/// reuses this renderer so "what you design is what the device shows" holds by
/// construction (never fork the renderer). Scaffolded by PROJ-101.
library;
